Metadata-Version: 2.4
Name: iotscan
Version: 0.1.0
Summary: IoT Security Pentesting Toolkit - Firmware analysis, protocol testing, credential checks, OTA analysis, and attack path mapping
Author: iotscan contributors
License: MIT
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: click>=8.1
Requires-Dist: rich>=13.0
Requires-Dist: paho-mqtt>=1.6
Requires-Dist: aiocoap>=0.4
Requires-Dist: bleak>=0.21
Requires-Dist: scapy>=2.5
Requires-Dist: pyyaml>=6.0
Requires-Dist: jinja2>=3.1
Requires-Dist: cryptography>=41.0
Requires-Dist: requests>=2.31
Requires-Dist: aiohttp>=3.9
Provides-Extra: dev
Requires-Dist: pytest>=7.4; extra == "dev"
Requires-Dist: pytest-asyncio>=0.21; extra == "dev"
Requires-Dist: pytest-cov>=4.1; extra == "dev"
Requires-Dist: ruff>=0.1; extra == "dev"

# iotscan

IoT Security Pentesting Toolkit — firmware analysis, protocol testing, credential checks, OTA update analysis, and device-to-cloud attack path mapping.

## Modules

| Module | Description |
|--------|-------------|
| **Firmware Analysis** | Binary header identification, hardcoded credential detection, unsafe C function scanning, vulnerable library detection, entropy analysis, debug artifact discovery |
| **Protocol Testing** | MQTT (anonymous access, TLS, wildcard topics, version), CoAP (resource discovery, DTLS), Zigbee (network key, security mode, permit join, touchlink), BLE (pairing, encryption, GATT, advertising) |
| **Credential Checker** | Default credential testing across HTTP, SSH, Telnet, FTP, and MQTT for 40+ IoT vendor defaults |
| **OTA Analyzer** | Update transport security, firmware signing verification, rollback protection, secure boot, certificate pinning, delta update validation |
| **Attack Path Mapper** | Device-to-cloud path identification, network segmentation checks, API security, cloud backend assessment, lateral movement analysis |

## Installation

```bash
pip install -e .
```

For development:
```bash
pip install -e ".[dev]"
```

## Usage

### Quick scan
```bash
iotscan scan 192.168.1.100
```

### Scan with specific protocol
```bash
iotscan scan 192.168.1.100 -p 1883 --protocol mqtt
```

### Firmware analysis
```bash
iotscan scan 192.168.1.100 --firmware ./firmware.bin -m firmware
```

### Full scan with config file
```bash
iotscan init-config config.yaml   # generate sample config
# edit config.yaml with your target details
iotscan scan 192.168.1.100 -c config.yaml -o report.html --format html
```

### List modules
```bash
iotscan list-modules
```

### Output formats
```bash
iotscan scan 192.168.1.100 -o report.txt --format text
iotscan scan 192.168.1.100 -o report.json --format json
iotscan scan 192.168.1.100 -o report.html --format html
```

## Configuration

Generate a sample config with `iotscan init-config config.yaml`. The config file supports:

- **target**: host, port, protocol, device type, firmware path
- **modules**: list of modules to run
- **config**: module-specific settings (Zigbee keys, BLE pairing mode, OTA URLs, ecosystem topology)

## Running Tests

```bash
pytest
```
